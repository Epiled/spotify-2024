{
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/server.js",
      "headers": {
        "Access-Control-Allow-Origin": "*"
      },
      "config": {
        "includeFiles": "db/playlist.json"
      }
    }
  ]
}
